name: Run Jest Tests

on: [push, pull_request]

jobs:
  test:
    name: Run Jest Tests
    runs-on: ubuntu-latest

    steps:
    

    - name: Install Dependencies
      run: npm install

    - name: Run Jest Tests
      run: npm test
